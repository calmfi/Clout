{
  "Queue": {
    // Note: JSON doesn't support comments at runtime; keep values minimal.
    // BasePath is where queue data is stored relative to the app base directory.
    "BasePath": "queue-data"
  }
}

